sudo: required

services:
  - docker

script:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
  - docker build -t raccourci/composer -t raccourci/composer:latest composer
  - docker push raccourci/composer
