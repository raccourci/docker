sudo: required

services:
  - docker

before_script:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD

script:
  - docker build --pull -t raccourci/puppet:debian-9.9 puppet/debian-9.9
  - docker push raccourci/puppet:debian-9.9
